import React from 'react';
import { render } from '@testing-library/react';
import PracticeEdit from './PracticeEdit';

describe('PracticeEdit Component', () => {
  it('renders the component without errors', () => {
    render(<PracticeEdit />);
   
  });
});